<UserControl x:Class="LollyWPF.TransformIntermediateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:LollyWPF"
             xmlns:common="clr-namespace:LollyCommon;assembly=LollyCommon"
             xmlns:mx="http://compiledbindings.com/x"
             mc:Ignorable="d mx"
             d:DataContext="{d:DesignInstance Type=local:TransformEditViewModelWPF}"
             mx:DataType="local:TransformEditViewModelWPF"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top">
            <xctk:IntegerUpDown Value="{x:Bind IntermediateIndex}" Width="75" VerticalAlignment="Center" Minimum="0" Maximum="{x:Bind IntermediateMaxIndex}"/>
        </StackPanel>
        <TextBox Text="{x:Bind IntermediateText}" IsReadOnly="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
    </DockPanel>
</UserControl>
