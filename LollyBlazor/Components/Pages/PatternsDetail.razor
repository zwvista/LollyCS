<MudDialog>
    <TitleContent>
        Dialog Title
    </TitleContent>
    <DialogContent>
        <MudTextField Value="@Item.ID" Label="ID" Variant="Variant.Text" Disabled="true"></MudTextField>
        <MudTextField @bind-Value="Item.PATTERN" Label="PATTERN" Variant="Variant.Text"></MudTextField>
        <MudTextField @bind-Value="Item.TAGS" Label="TAGS" Variant="Variant.Text"></MudTextField>
        <MudTextField @bind-Value="Item.TITLE" Label="TITLE" Variant="Variant.Text"></MudTextField>
        <MudTextField @bind-Value="Item.URL" Label="URL" Variant="Variant.Text"></MudTextField>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="@Submit">OK</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] 
    IMudDialogInstance MudDialog { get; set; }
    [Parameter]
    public PatternsDetailViewModel vmDetail { get; set; }
    MPatternEdit Item => vmDetail.ItemEdit;

    void Submit() => MudDialog.Close(DialogResult.Ok(true));
    void Cancel() => MudDialog.Cancel();
}
