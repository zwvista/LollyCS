<MudDialog>
    <TitleContent>
        Dialog Title
    </TitleContent>
    <DialogContent>
        <MudTextField Value="@Item.ID" Label="ID" Variant="Variant.Text" Disabled="true"></MudTextField>
        <MudTextField Value="@Item.WORD" Label="WORD" Variant="Variant.Text"></MudTextField>
        <MudTextField Value="@Item.NOTE" Label="NOTE" Variant="Variant.Text"></MudTextField>
        <MudTextField Value="@Item.FAMIID" Label="FAMIID" Variant="Variant.Text" Disabled="true"></MudTextField>
        <MudTextField Value="@Item.ACCURACY" Label="ACCURACY" Variant="Variant.Text" Disabled="true"></MudTextField>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="@Submit">OK</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] 
    IMudDialogInstance MudDialog { get; set; }
    [Parameter]
    public MLangWord Item { get; set; }

    void Submit() => MudDialog.Close(DialogResult.Ok(true));
    void Cancel() => MudDialog.Cancel();
}
